import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

const MenuCategory = () => {
  return (
    <Box>
      <Typography variant="h4">Menu Category Page</Typography>
    </Box>
  );
};

export default MenuCategory;
